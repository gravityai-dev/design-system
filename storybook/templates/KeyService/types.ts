import type { GravityTemplateProps } from "../core";

/**
 * KeyService Template Props
 * Simple 2-column split: content left, image right
 */
export interface KeyServiceProps extends GravityTemplateProps {
  // No additional props - uses base template props
}
